<li>
    <% if ((locals.user) && (locals.user.id == comment.user.id)){ %> 
        <small>
            <a href="/comments/destroy/<%= comment.id %>">XComment</a>
        </small>
        <%} else if ((locals.user) && ( locals.user.id == post.user.id)){ %> 
            <small>
                <a href="/comments/destroy/<%= comment.id %>">XPost</a>
            </small>
            <%}%>
    <h3>
         <%= comment.content %>
         <br>
         <small>
            <%= comment.user.email %>
         </small>
        </h3>

</li>